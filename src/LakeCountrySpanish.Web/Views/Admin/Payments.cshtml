@model LakeCountrySpanish.Web.Models.ViewModels.AdminPaymentsViewModel
@{
    ViewData["Title"] = "Payments";
}

<div class="bg-gray-50 min-h-screen">
    <div class="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8">
        <div class="mb-8">
            <h1 class="text-3xl font-bold text-gray-900">Payments</h1>
            <p class="mt-2 text-gray-600">Track all student payments.</p>
        </div>

        <!-- Stats -->
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-6 mb-8">
            <div class="bg-white rounded-lg shadow p-6">
                <p class="text-sm text-gray-500">Total Revenue (filtered)</p>
                <p class="text-3xl font-bold text-green-600">$@Model.TotalRevenue.ToString("0.00")</p>
            </div>
            <div class="bg-white rounded-lg shadow p-6">
                <p class="text-sm text-gray-500">Pending Payments</p>
                <p class="text-3xl font-bold text-yellow-600">$@Model.PendingPayments.ToString("0.00")</p>
            </div>
        </div>

        <!-- Filters -->
        <div class="bg-white rounded-lg shadow p-4 mb-6">
            <form asp-action="Payments" method="get" class="flex flex-wrap gap-4">
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Start Date</label>
                    <input type="date" name="startDate" value="@Model.StartDate?.ToString("yyyy-MM-dd")"
                           class="rounded-md border-gray-300 shadow-sm focus:border-primary-500 focus:ring-primary-500 px-4 py-2 border" />
                </div>
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">End Date</label>
                    <input type="date" name="endDate" value="@Model.EndDate?.ToString("yyyy-MM-dd")"
                           class="rounded-md border-gray-300 shadow-sm focus:border-primary-500 focus:ring-primary-500 px-4 py-2 border" />
                </div>
                <div>
                    <label class="block text-sm font-medium text-gray-700 mb-1">Student</label>
                    <select name="studentId" class="rounded-md border-gray-300 shadow-sm focus:border-primary-500 focus:ring-primary-500 px-4 py-2 border">
                        <option value="">All Students</option>
                        @foreach (var student in Model.Students)
                        {
                            <option value="@student.Id" selected="@(Model.StudentId == student.Id)">@student.FullName</option>
                        }
                    </select>
                </div>
                <div class="flex items-end">
                    <button type="submit" class="bg-gray-800 hover:bg-gray-900 text-white rounded-lg px-4 py-2 font-medium transition">
                        Filter
                    </button>
                </div>
            </form>
        </div>

        <!-- Payments List -->
        <div class="bg-white rounded-lg shadow overflow-hidden">
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                    <tr>
                        <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Date</th>
                        <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Student</th>
                        <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Description</th>
                        <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Amount</th>
                        <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Status</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200">
                    @foreach (var payment in Model.Payments)
                    {
                        <tr class="hover:bg-gray-50">
                            <td class="px-6 py-4 whitespace-nowrap">
                                <p class="text-sm text-gray-900">@payment.CreatedAt.ToString("MMM d, yyyy")</p>
                                <p class="text-xs text-gray-500">@payment.CreatedAt.ToString("h:mm tt")</p>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <a asp-action="StudentProfile" asp-route-id="@payment.StudentId" class="text-sm text-primary-600 hover:text-primary-700 hover:underline">@payment.Student.FullName</a>
                            </td>
                            <td class="px-6 py-4">
                                <p class="text-sm text-gray-900">@payment.Description</p>
                                <p class="text-xs text-gray-500">@payment.PaymentType</p>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <p class="text-sm font-medium text-gray-900">$@payment.Amount.ToString("0.00")</p>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium
                                    @(payment.Status == LakeCountrySpanish.Web.Models.Entities.PaymentStatusType.Completed ? "bg-green-100 text-green-800" :
                                      payment.Status == LakeCountrySpanish.Web.Models.Entities.PaymentStatusType.Pending ? "bg-yellow-100 text-yellow-800" :
                                      payment.Status == LakeCountrySpanish.Web.Models.Entities.PaymentStatusType.Failed ? "bg-red-100 text-red-800" :
                                      "bg-gray-100 text-gray-800")">
                                    @payment.Status
                                </span>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
            @if (!Model.Payments.Any())
            {
                <div class="text-center py-12">
                    <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M2.25 8.25h19.5M2.25 9h19.5m-16.5 5.25h6m-6 2.25h3m-3.75 3h15a2.25 2.25 0 002.25-2.25V6.75A2.25 2.25 0 0019.5 4.5h-15a2.25 2.25 0 00-2.25 2.25v10.5A2.25 2.25 0 004.5 19.5z" />
                    </svg>
                    <p class="mt-2 text-gray-500">No payments found</p>
                </div>
            }
        </div>
    </div>
</div>
